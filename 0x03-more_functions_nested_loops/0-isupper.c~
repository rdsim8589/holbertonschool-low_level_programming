#include "holberton.h"

/**
 * int 0_isupper - checks if the character is upper case
 * @c: an int
 *
 * Return: 1 if c if upper case
 * 0 if not upper case
 */

int 0_isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
