#include "holberton.h"

/**
 * _abs - computes the absolute value of a function
 * @n: any int value
 *
 * Return: the absolute value of the variable
 */

int _abs(int n)
{
	if (n < 0)
	{
		n *= -1;
		return (n);
	}
	else
	{
		return (n);
	}
}
